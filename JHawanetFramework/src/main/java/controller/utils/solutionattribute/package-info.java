/**
 * This packages contains attributes used by the GUI to save some values in the solution.
 * <p>
 * In algorithms, problems or operators you don't should use this attribute because it can be overwritten by the
 * GUI when it execute the algorithm.
 */
package controller.utils.solutionattribute;